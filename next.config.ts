import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["developers.elementor.com"], // Add other domains as needed
  },
};

export default nextConfig;
